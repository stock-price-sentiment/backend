version: '3'
services:
  web:
    build: .
    ports: 
      - "5000:5000"
    volumes:
      - .:/code
    environment: 
      FLASK_ENV: development
  postgres:
    image: "postgres:latest"
    ports: 
      - "5432:5432"
    environment: 
      POSTGRES_PASSWORD: password 